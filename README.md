# Distributed SGD with PyTorch DDP – ML Systems Assignment

Single-process baseline implementation of data-parallel SGD using PyTorch on CIFAR-10 with ResNet-18.

- Reduced dataset (~5,000 images) for fast demo execution
- Measured training time for 1 epoch (N=1)
